# Пошаговая установка хардвера - RAMS Interactive Hub

## 🎯 Что мы будем делать

Подключить и настроить:
- ✅ ESP32 контроллер
- ✅ WS2812B LED ленты 12V (подсветка проектов)
- ✅ Реле модуль 5V/10A+ (для дополнительного освещения)
- ✅ Актуаторы 12V (опционально)
- ✅ WiFi связь с компьютером

**Время установки**: 3-5 часов
**Уровень сложности**: Средний

---

## 📦 ШАГ 0: Проверка компонентов

### Что должно быть у вас:

#### Обязательные компоненты:
- [ ] ESP32 DevKit (любая версия)
- [ ] WS2812B LED лента 12V (количество метров: ____ )
- [ ] Блок питания 12V/10A (или больше)
- [ ] USB кабель для ESP32 (Micro-USB или USB-C)
- [ ] USB адаптер 5V/2A для ESP32
- [ ] Реле модуль 4-канальный (5V логика, 12V/10A нагрузка)
- [ ] Level Shifter 74HCT125 (для Data line 3.3V → 5V)
- [ ] Провода: красный (1.5мм²), черный (1.5мм²), зеленый (0.75мм²)
- [ ] Dupont кабели (M-M, M-F)

#### Опциональные:
- [ ] L298N H-Bridge (для актуаторов)
- [ ] Linear Actuator 12V
- [ ] Конденсаторы 1000μF
- [ ] Предохранители 5A, 10A

#### Инструменты:
- [ ] Мультиметр
- [ ] Паяльник (опционально)
- [ ] Отвертки
- [ ] Кусачки, стриппер
- [ ] Стяжки кабельные

---

## 🔧 ШАГ 1: Подготовка ESP32

### 1.1. Проверка ESP32

1. **Подключить ESP32 к компьютеру через USB**:
   ```
   Компьютер USB ──► USB кабель ──► ESP32
   ```

2. **Проверить что компьютер видит ESP32**:
   - **Windows**: Диспетчер устройств → Порты (COM и LPT) → должен быть "USB Serial Port (COM3)" или подобное
   - **Mac**: Terminal → `ls /dev/tty.*` → должно быть `/dev/tty.usbserial-xxx`
   - **Linux**: Terminal → `ls /dev/ttyUSB*` → должно быть `/dev/ttyUSB0`

3. **Если не видит ESP32**:
   - Установить драйвер: CP210x или CH340
   - Попробовать другой USB кабель
   - Попробовать другой USB порт

### 1.2. Установка Arduino IDE / PlatformIO

Выбрать один вариант:

#### Вариант A: Arduino IDE (проще)

1. Скачать: https://www.arduino.cc/en/software
2. Установить ESP32 Board:
   - File → Preferences → Additional Board Manager URLs:
   ```
   https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
   ```
   - Tools → Board → Boards Manager → Искать "esp32" → Install

3. Установить библиотеки:
   - Tools → Manage Libraries:
     - `FastLED` by Daniel Garcia
     - `ArduinoJson` by Benoit Blanchon
     - `ESPAsyncWebServer` by me-no-dev
     - `AsyncTCP` by me-no-dev

#### Вариант B: PlatformIO (рекомендуется для опытных)

1. Установить VS Code
2. Extensions → Искать "PlatformIO IDE" → Install
3. Готово! (библиотеки установятся автоматически)

### 1.3. Загрузка тестовой прошивки

1. **Открыть файл**: `rams_controller.ino`

2. **Настроить параметры**:
   ```cpp
   // В начале файла
   const char* WIFI_SSID = "ВАША_СЕТЬ";
   const char* WIFI_PASSWORD = "ВАШ_ПАРОЛЬ";

   #define NUM_LEDS 10  // Пока только 10 для теста
   ```

3. **Выбрать порт**:
   - Tools → Port → (выбрать COM порт ESP32)

4. **Загрузить**:
   - Нажать кнопку Upload (→)
   - Дождаться "Done uploading"

5. **Открыть Serial Monitor**:
   - Tools → Serial Monitor
   - Выбрать 115200 baud
   - Должны увидеть:
   ```
   ========================================
     RAMS Interactive Hub - ESP32
     Hardware Controller v1.0
   ========================================
   [SETUP] Configuring GPIO pins...
   [SETUP] GPIO pins configured ✓
   ...
   [WIFI] Connected successfully! ✓
   [WIFI] IP Address: 192.168.1.100
   ```

6. **Проверить WiFi**:
   - Открыть браузер
   - Перейти: `http://192.168.1.100`
   - Должна открыться страница "RAMS Interactive Hub"

✅ **ESP32 готов!**

---

## 💡 ШАГ 2: Подключение первого LED сегмента (ТЕСТ)

**ВАЖНО**: Сначала тестируем на 1 метре LED ленты!

### 2.1. Подготовка LED ленты

1. **Отрезать 1 метр LED ленты** (для теста)

2. **Определить направление**:
   - Найти стрелки на ленте: ` ──► ──► ──► `
   - Найти маркировку:
     - `DIN` или `DATA IN` = вход
     - `DOUT` или `DATA OUT` = выход

3. **Зачистить провода** (если нужно):
   - Красный: +12V
   - Черный: GND
   - Зеленый/Белый: DATA

### 2.2. Подключение Level Shifter

**Зачем**: ESP32 = 3.3V логика, WS2812B = 5V логика. Level Shifter преобразует 3.3V → 5V.

#### Схема 74HCT125:

```
┌──────────────────────────────────────┐
│         74HCT125 (или модуль)        │
│                                      │
│  VCC (Pin 14) ◄─── 5V (от USB ESP32)│
│  GND (Pin 7)  ◄─── GND               │
│  /OE (Pin 1)  ◄─── GND (всегда LOW) │
│                                      │
│  A (Pin 2)    ◄─── ESP32 GPIO 16     │
│  Y (Pin 3)    ───► WS2812B DATA IN   │
└──────────────────────────────────────┘
```

#### Подключение Dupont кабелями:

1. **ESP32 5V → Level Shifter VCC**
2. **ESP32 GND → Level Shifter GND**
3. **ESP32 GPIO 16 → Level Shifter A (Input)**
4. **Level Shifter Y (Output) → WS2812B DATA IN (зеленый провод)**

### 2.3. Подключение питания LED

```
┌─────────────────────────────────────────┐
│         Блок питания 12V/10A            │
└───────┬─────────────────────┬───────────┘
        │ +12V (красный)      │ GND (черный)
        │                     │
        ▼                     ▼
   WS2812B Red Wire      WS2812B Black Wire
        │                     │
        │                     └──┐
        └────────────────────────┼──► ESP32 GND
                              (ОБЩАЯ ЗЕМЛЯ!)
```

**КРИТИЧНО**: ESP32 GND и БП 12V GND ДОЛЖНЫ быть соединены!

### 2.4. Схема подключения (полная)

```
┌──────────────┐
│   БП 12V     │
│              │
│ +12V ────────┼──────────► WS2812B +12V (красный)
│ GND ─────────┼──────┬───► WS2812B GND (черный)
└──────────────┘      │
                      │ (ОБЩАЯ ЗЕМЛЯ)
┌──────────────┐      │
│   ESP32      │      │
│              │      │
│ 5V ──┐       │      │
│ GND ─┼───────┼──────┴───► Level Shifter GND
│ GPIO16 ──────┼──► Level Shifter Input (A)
└──────────────┘
                      │
┌──────────────┐      │
│ Level Shifter│      │
│              │      │
│ VCC ◄─── 5V (от ESP32)
│ GND ◄────────┼──────┘
│ Output (Y) ──┼──────────► WS2812B DATA IN (зеленый)
└──────────────┘
```

### 2.5. Первый тест

1. **Подключить все провода согласно схеме**

2. **Проверить мультиметром**:
   - +12V на LED ленте: должно быть 11.8-12.3V
   - GND: должна быть непрерывность между ESP32 GND и БП GND

3. **Включить БП 12V**

4. **Подключить ESP32 к USB (питание)**

5. **Загрузить тестовый код** (уже должен быть загружен с Шага 1)

6. **Отправить команду через браузер или curl**:

   **Браузер**:
   ```
   http://192.168.1.100/command
   ```

   **curl** (в терминале):
   ```bash
   curl -X POST http://192.168.1.100/command \
     -H "Content-Type: application/json" \
     -d '{"action":"highlight","projectId":0,"data":{"color":"#FF0000","brightness":200}}'
   ```

7. **Результат**:
   - ✅ LED должны загореться КРАСНЫМ цветом
   - ✅ Яркость должна быть высокой
   - ❌ Если не работает → см. раздел "Troubleshooting" ниже

### 2.6. Тест других цветов

Попробуйте разные цвета:

```bash
# Синий
curl -X POST http://192.168.1.100/command \
  -H "Content-Type: application/json" \
  -d '{"action":"highlight","projectId":0,"data":{"color":"#0000FF"}}'

# Зеленый
curl -X POST http://192.168.1.100/command \
  -H "Content-Type: application/json" \
  -d '{"action":"highlight","projectId":0,"data":{"color":"#00FF00"}}'

# Белый
curl -X POST http://192.168.1.100/command \
  -H "Content-Type: application/json" \
  -d '{"action":"highlight","projectId":0,"data":{"color":"#FFFFFF"}}'

# Выключить
curl -X POST http://192.168.1.100/command \
  -H "Content-Type: application/json" \
  -d '{"action":"off","projectId":0}'
```

✅ **LED работают! Переходим к следующему шагу.**

---

## 🔗 ШАГ 3: Подключение всех LED лент

### 3.1. Планирование

1. **Посчитать количество проектов**: _____ шт

2. **Измерить LED на каждый проект**:
   - Проект 0: _____ LED (÷3 = _____ адресов)
   - Проект 1: _____ LED (÷3 = _____ адресов)
   - ...

3. **Рассчитать общее количество адресов**: _____

### 3.2. Выбор схемы подключения

Выбрать из `LED-LAYOUT-GUIDE.md`:

- [ ] **Последовательная** (простая, для маленьких макетов)
- [ ] **Звезда** (для больших, независимые линии)
- [ ] **Гибридная** (рекомендуется, баланс)

### 3.3. Физическое подключение

1. **Соединить LED ленты согласно выбранной схеме**

2. **Подключить DATA line**:
   - От Level Shifter → Первый проект DATA IN
   - От Проект 0 DATA OUT → Проект 1 DATA IN
   - От Проект 1 DATA OUT → Проект 2 DATA IN
   - ...

3. **Подключить питание 12V**:
   - **ВАЖНО**: Подавать питание каждые 150-200 LED!
   ```
   БП 12V ──┬──► Проект 0 (+12V, GND)
            ├──► Проект 3 (+12V, GND)  // Инжекция
            └──► Проект 6 (+12V, GND)  // Инжекция
   ```

4. **Общая земля**:
   - Все GND должны быть соединены:
     - ESP32 GND
     - БП 12V GND
     - Каждый проект GND

### 3.4. Обновление конфигурации

Отредактировать `PROJECT-CONFIG-EXAMPLE.h`:

```cpp
// Пример
projects[0].ledStart = 0;
projects[0].ledCount = 10;  // 30 физических LED ÷ 3

projects[1].ledStart = 10;
projects[1].ledCount = 15;  // 45 физических LED ÷ 3

projects[2].ledStart = 25;
projects[2].ledCount = 8;   // 24 физических LED ÷ 3

// И так далее...
```

Обновить `NUM_LEDS`:

```cpp
#define NUM_LEDS 200  // Общее количество АДРЕСОВ (не физических LED!)
```

### 3.5. Загрузка новой конфигурации

1. **Включить конфигурацию в коде**:
   ```cpp
   #include "PROJECT-CONFIG-EXAMPLE.h"
   ```

2. **Загрузить на ESP32**

3. **Проверить Serial Monitor**:
   ```
   [SETUP] Configured 28 projects ✓
   Total LEDs (addresses): 200
   Physical LEDs: 600
   ```

### 3.6. Тест каждого проекта

Тестировать по очереди:

```bash
# Проект 0
curl -X POST http://192.168.1.100/command \
  -d '{"action":"highlight","projectId":0}'

# Проект 1
curl -X POST http://192.168.1.100/command \
  -d '{"action":"highlight","projectId":1}'

# ...
```

Проверить что:
- ✅ Правильный проект загорается
- ✅ Цвет соответствует ожиданию
- ✅ Все LED в проекте работают

✅ **Все LED подключены и работают!**

---

## ⚡ ШАГ 4: Подключение реле модуля

### 4.1. О вашем реле

У вас есть: **Реле 5V логика, 10A+**

Это отличный модуль! 10A достаточно для:
- LED лент 12V
- Ламп 12V/50W
- Вентиляторов 12V
- Других 12V устройств

### 4.2. Схема подключения реле

```
┌──────────────────────────────────────┐
│     Relay Module 4-Channel           │
│     (5V logic, 12V/10A per channel)  │
│                                      │
│  VCC  ◄─── ESP32 5V (или USB 5V)    │
│  GND  ◄─── ESP32 GND                 │
│                                      │
│  IN1  ◄─── ESP32 GPIO 17             │
│  IN2  ◄─── ESP32 GPIO 18             │
│  IN3  ◄─── ESP32 GPIO 19             │
│  IN4  ◄─── ESP32 GPIO 23             │
│                                      │
│  COM1 ◄─── БП 12V (+)                │
│  NO1  ───► Лампа/LED (+)             │
│  NC1  (не используется)              │
│                                      │
│  Лампа (-) ──► БП 12V (-)            │
└──────────────────────────────────────┘
```

### 4.3. Подключение Dupont кабелями

1. **Питание реле**:
   - ESP32 5V → Relay VCC
   - ESP32 GND → Relay GND

2. **Управляющие сигналы**:
   - ESP32 GPIO 17 → Relay IN1
   - ESP32 GPIO 18 → Relay IN2
   - ESP32 GPIO 19 → Relay IN3
   - ESP32 GPIO 23 → Relay IN4

3. **Нагрузка 12V** (пример для канала 1):
   - БП 12V (+) → Relay COM1
   - Relay NO1 → Лампа/LED (+)
   - Лампа/LED (-) → БП 12V (-)

### 4.4. Тест реле

Отправить команду:

```bash
# Включить реле #1
curl -X POST http://192.168.1.100/command \
  -H "Content-Type: application/json" \
  -d '{"action":"toggle_element","target":"light1","state":"true"}'

# Выключить реле #1
curl -X POST http://192.168.1.100/command \
  -d '{"action":"toggle_element","target":"light1","state":"false"}'
```

**Ожидаемый результат**:
- ✅ Слышен щелчок реле (клац!)
- ✅ LED на реле модуле загорается
- ✅ Подключенная лампа/нагрузка включается

### 4.5. Подключение нагрузки к реле

**Примеры нагрузки**:

#### A. Дополнительная LED лента 12V

```
БП 12V (+) ──► Relay COM
              Relay NO ──► LED Strip (+)
                          LED Strip (-) ──► БП 12V (-)
```

#### B. Лампа 12V/20W

```
БП 12V (+) ──► Relay COM
              Relay NO ──► Лампа (+)
                          Лампа (-) ──► БП 12V (-)
```

#### C. Вентилятор 12V

```
БП 12V (+) ──► Relay COM
              Relay NO ──► Вентилятор (+)
                          Вентилятор (-) ──► БП 12V (-)
```

### 4.6. Расчет нагрузки

**Максимум на 1 канал**: 10A × 12V = 120W

**Примеры**:
- LED лента 12V: ~10W/м → макс. 12 метров на канал
- Лампа 12V/50W: можно 2 лампы на канал
- Вентилятор 12V/5W: можно 24 штуки на канал

✅ **Реле подключены и работают!**

---

## 🚀 ШАГ 5: Подключение актуаторов (опционально)

### 5.1. Если у вас есть актуаторы

Для управления линейными актуаторами 12V нужен **H-Bridge** (например, L298N).

### 5.2. Схема подключения L298N

```
┌───────────────────────────────────────────┐
│            L298N H-Bridge                 │
│                                           │
│  +12V ◄─── БП 12V (+)                     │
│  GND  ◄─── БП 12V (-) и ESP32 GND        │
│  +5V  ──X (НЕ подключать к ESP32!)       │
│                                           │
│  IN1  ◄─── ESP32 GPIO 21 (Actuator Forward) │
│  IN2  ◄─── ESP32 GPIO 22 (Actuator Reverse) │
│                                           │
│  OUT1,OUT2 ──► Actuator Motor Wires      │
└───────────────────────────────────────────┘
```

### 5.3. Подключение

1. **Питание**:
   - БП 12V (+) → L298N +12V
   - БП 12V (-) → L298N GND
   - ESP32 GND → L298N GND (общая земля!)

2. **Управление**:
   - ESP32 GPIO 21 → L298N IN1
   - ESP32 GPIO 22 → L298N IN2

3. **Мотор актуатора**:
   - Actuator Wire 1 → L298N OUT1
   - Actuator Wire 2 → L298N OUT2

### 5.4. Тест актуатора

```bash
# Двигать вверх
curl -X POST http://192.168.1.100/command \
  -d '{"action":"actuator","target":"roof","state":"up"}'

# Стоп (через 5 секунд автоматически)

# Двигать вниз
curl -X POST http://192.168.1.100/command \
  -d '{"action":"actuator","target":"roof","state":"down"}'

# Стоп немедленно
curl -X POST http://192.168.1.100/command \
  -d '{"action":"actuator","target":"roof","state":"stop"}'
```

✅ **Актуаторы подключены и работают!**

---

## 🧪 ШАГ 6: Финальное тестирование

### 6.1. Чек-лист

Проверить все функции:

- [ ] WiFi подключение (IP: 192.168.1.100)
- [ ] Ping endpoint: `http://192.168.1.100/ping`
- [ ] Status endpoint: `http://192.168.1.100/status`
- [ ] LED: каждый проект загорается отдельно
- [ ] LED: правильные цвета
- [ ] LED: эффекты работают (fade, pulse)
- [ ] Реле: включаются/выключаются
- [ ] Актуаторы: двигаются вверх/вниз (если есть)
- [ ] Сброс: команда `reset` выключает все

### 6.2. Тест из Electron приложения

1. **Запустить приложение** RAMS Interactive Hub

2. **Открыть проект** (кликнуть на карточку)

3. **Проверить**:
   - ✅ Соответствующий проект на макете загорается
   - ✅ Цвет соответствует статусу проекта
   - ✅ Эффект плавного включения (fade)

4. **Переключить проект**:
   - ✅ Предыдущий проект выключается
   - ✅ Новый проект загорается

5. **Закрыть приложение**:
   - ✅ Все LED выключаются

### 6.3. Нагрузочный тест

Проверить стабильность при максимальной нагрузке:

1. **Включить все проекты одновременно**:
   ```bash
   for i in {0..27}; do
     curl -X POST http://192.168.1.100/command \
       -d "{\"action\":\"highlight\",\"projectId\":$i}"
     sleep 0.1
   done
   ```

2. **Проверить**:
   - Температура БП (не должен перегреваться)
   - Стабильность питания (мультиметр на LED)
   - Яркость LED (должна быть одинаковая)

3. **Работать 30 минут**:
   - Проверить что ничего не греется
   - ESP32 не зависает
   - WiFi не отключается

✅ **Все работает стабильно!**

---

## 🐛 Troubleshooting

### LED не загораются

| Проблема | Решение |
|----------|---------|
| Совсем не горят | Проверить питание 12V на ленте |
| Первый LED горит, остальные нет | Проверить Data провод, Level Shifter |
| Неправильные цвета | Изменить `COLOR_ORDER` в коде (GRB ↔ RGB) |
| Мерцают | Добавить конденсатор 1000μF на питание |
| Тусклые | Увеличить `BRIGHTNESS` в коде |

### ESP32 не подключается к WiFi

1. Проверить SSID и пароль
2. Убедиться что роутер WiFi включен
3. Попробовать другую сеть
4. Проверить Serial Monitor на ошибки

### Реле не срабатывают

1. Проверить что VCC = 5V (мультиметр)
2. Проверить GPIO сигнал (мультиметр на IN pin)
3. Попробовать другой канал реле
4. Проверить тип реле (активное LOW/HIGH)

### Актуаторы не двигаются

1. Проверить питание 12V на L298N
2. Проверить GPIO сигналы на IN1, IN2
3. Измерить напряжение на OUT1, OUT2 (должно быть 12V)
4. Попробовать подключить актуатор напрямую к 12V (тест мотора)

---

## 📸 Фото документация

**Рекомендуется сфотографировать**:
1. Общий вид установки
2. ESP32 с подключенными проводами
3. Level Shifter подключение
4. Реле модуль подключение
5. Маркировку проводов

---

## ✅ Финальный чек-лист

Перед началом эксплуатации:

- [ ] Все провода надежно закреплены
- [ ] Нет оголенных контактов
- [ ] БП стоит на устойчивой поверхности
- [ ] Вентиляция БП не закрыта
- [ ] Общая земля (GND) везде подключена
- [ ] Предохранители установлены
- [ ] WiFi работает стабильно
- [ ] Все функции протестированы
- [ ] Запасные компоненты подготовлены
- [ ] Документация сохранена

---

## 🎉 Готово!

Теперь у вас полностью рабочая интерактивная система управления макетом!

**Следующие шаги**:
1. Калибровка яркости и цветов
2. Настройка эффектов для каждого проекта
3. Обучение персонала работе с системой
4. Регулярное обслуживание (проверка контактов, чистка)

---

**Дата**: 2026-01-27
**Версия**: 1.0
**Для проекта**: RAMS Interactive Hub

**Поддержка**: См. `/docs/` для дополнительной документации

**Если что-то не работает**: Проверьте Serial Monitor ESP32 для логов ошибок!
