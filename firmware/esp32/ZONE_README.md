# RAMS Screen - Zone-Based LED Architecture

## Концепция

Это **профессиональная архитектура** для управления сложной кинетической инсталляцией с зональной подсветкой.

### Ключевое отличие от обычного подхода:

**Обычный подход**: 1 проект = 1 непрерывная лента LED
**Наш подход**: 1 зона = несколько сегментов на РАЗНЫХ физических лентах

## Структура проекта

```
firmware/esp32/
├── rams_screen/
│   └── rams_screen.ino          # Главный скетч
├── ZONE_CONFIG.h                 # Конфигурация зон и оборудования
├── ZONE_CONTROLLER.h             # Библиотека управления
└── ZONE_README.md                # Эта документация
```

## Архитектура системы

### 1. Физические линии LED (10 шт)

```
Line 0-7: Радиальные лучи (от центра к краю)
Line 8:   Внутренний круг (концентрический)
Line 9:   Внешний круг (концентрический)
```

**Каждая линия:**
- Подключена к отдельному GPIO ESP32
- Имеет свой массив `CRGB` в памяти
- Управляется независимо через FastLED

### 2. Логические зоны (15 шт)

Зона = **визуальный сектор** инсталляции

**Пример: Зона 0** (сектор между лучом 1 и лучом 2)
```cpp
LEDSegment zone0_segments[] = {
  LEDSegment(0, 0, 30),      // Часть луча 1
  LEDSegment(1, 0, 30),      // Часть луча 2
  LEDSegment(8, 0, 20),      // Часть внутреннего круга
  LEDSegment(9, 0, 25)       // Часть внешнего круга
};
```

При команде `ZoneController::setZoneColor(0, CRGB::Red)` загорается **вся зона** - все 4 сегмента одновременно.

### 3. Актуаторы (31 шт)

31 линейный актуатор расположены в точках пересечения лучей и кругов.

**Управление:**
- Каждый актуатор = 2 реле (H-мост)
- Всего 62 реле = 15 модулей по 4 канала + 1 модуль с 2 каналами

**Автоматический таймер:**
```cpp
ActuatorController::moveForward(5, 3000);  // Двигаться 3 секунды
// Актуатор автоматически остановится через 3 сек
```

## Установка и настройка

### Шаг 1: Установка зависимостей

В Arduino IDE установите библиотеки:
- **FastLED** (версия 3.6+)
- **ESPAsyncWebServer**
- **ArduinoJson** (версия 6+)

### Шаг 2: Подключение оборудования

#### ESP32 GPIO маппинг

```cpp
// В файле ZONE_CONFIG.h настройте пины:

const uint8_t LED_PINS[10] = {
  16, 17, 18, 19, 21,  // Лучи 1-5
  22, 23, 25, 26, 27   // Лучи 6-8 + 2 круга
};
```

#### Схема подключения LED

```
ESP32 GPIO 16 → Ray 1 Data In
ESP32 GPIO 17 → Ray 2 Data In
...
ESP32 GPIO 27 → Outer Circle Data In

Все ленты:
- Питание: 12V (от отдельного БП)
- GND: Общая земля с ESP32
```

#### Схема подключения актуаторов

```
Актуатор → H-мост (L298N или реле) → ESP32

Пример для актуатора #0:
GPIO 32 → Реле Forward
GPIO 33 → Реле Backward
```

### Шаг 3: Инвентаризация LED

**КРИТИЧЕСКИ ВАЖНО!** Нужно измерить реальное количество LED на каждой линии.

```cpp
// В ZONE_CONFIG.h:
const uint16_t LEDS_PER_LINE[10] = {
  100,  // TODO: Измерить Ray 1
  100,  // TODO: Измерить Ray 2
  ...
};
```

**Как измерить:**
1. Загрузите тестовый скетч (см. ниже)
2. Включайте ленты по одной
3. Считайте светящиеся пиксели
4. Обновите массив `LEDS_PER_LINE`

### Шаг 4: Настройка сегментов зон

После инвентаризации заполните **реальные индексы** сегментов:

```cpp
// Пример для Зоны 0
LEDSegment zone0_segments[] = {
  LEDSegment(0, 0, 45),      // Ray 1: пиксели 0-45
  LEDSegment(1, 0, 45),      // Ray 2: пиксели 0-45
  LEDSegment(8, 0, 28),      // Inner Circle: пиксели 0-28
  LEDSegment(9, 0, 35)       // Outer Circle: пиксели 0-35
};
```

**Визуализация:**
```
      Ray 1 (Line 0)
           |
           |  ← Зона 0 содержит
    -------*-------  часть Ray 1
   (       ^       )
  ( Inner  |Outer  )  и части обоих кругов
   (Circle | Circle)
    -------*-------
           |  ← и часть Ray 2
           |
      Ray 2 (Line 1)
```

### Шаг 5: Загрузка прошивки

1. Откройте `rams_screen/rams_screen.ino` в Arduino IDE
2. Выберите плату: **ESP32 Dev Module**
3. Настройки:
   - CPU Frequency: 240MHz
   - Flash Size: 4MB
   - Partition Scheme: Default
4. Загрузите на ESP32

## Использование

### API через HTTP

ESP32 создает WiFi точку доступа:
```
SSID: RAMS_Screen
Password: RamsScreen2026
IP: 192.168.4.1
```

### Примеры команд

#### 1. Подсветить зону

```bash
curl -X POST http://192.168.4.1/zone \
  -H "Content-Type: application/json" \
  -d '{
    "action": "set",
    "zoneId": 0,
    "color": "#FF0000",
    "brightness": 200,
    "effect": "fade"
  }'
```

#### 2. Выключить зону

```bash
curl -X POST http://192.168.4.1/zone \
  -H "Content-Type: application/json" \
  -d '{
    "action": "clear",
    "zoneId": 0
  }'
```

#### 3. Управление актуатором

```bash
# Двигать вперед 3 секунды
curl -X POST http://192.168.4.1/actuator \
  -H "Content-Type: application/json" \
  -d '{
    "id": 5,
    "direction": "forward",
    "duration": 3000
  }'

# Остановить
curl -X POST http://192.168.4.1/actuator \
  -H "Content-Type: application/json" \
  -d '{
    "id": 5,
    "direction": "stop"
  }'
```

#### 4. Тест всех зон

```bash
curl http://192.168.4.1/test
```

#### 5. Выключить всё

```bash
curl http://192.168.4.1/clear
```

### Использование в коде

```cpp
#include "ZONE_CONFIG.h"
#include "ZONE_CONTROLLER.h"

void setup() {
  // Инициализация
  ZoneController::init();
  ActuatorController::init();

  // Подсветить зону 5 красным
  ZoneController::setZoneColor(5, CRGB::Red);

  // Плавное появление синего
  ZoneController::fadeInZone(3, CRGB::Blue, 1000);

  // Пульсация зелёным
  ZoneController::pulseZone(7, CRGB::Green);

  // Двигать актуатор
  ActuatorController::moveForward(10, 2000);
}

void loop() {
  // Обязательно! Автостоп актуаторов
  ActuatorController::update();
}
```

## Отладка

### Serial Monitor

При загрузке прошивки откройте Serial Monitor (115200 baud):

```
========================================
    RAMS SCREEN - Kinetic Installation
    Zone-Based LED Controller v2.0
========================================
[ZONE] Initializing FastLED for all lines...
[ZONE] Initialized 10 physical LED lines ✓

========================================
  ZONE-BASED LED CONFIGURATION
========================================
Physical Lines: 10
Logical Zones: 15
Actuators: 31

Zone Details:
  Zone 0 [Sector 1-2]: 4 segments, 126 total pixels
    - Line 0: pixels 0-30 (31 LEDs)
    - Line 1: pixels 0-30 (31 LEDs)
    - Line 8: pixels 0-20 (21 LEDs)
    - Line 9: pixels 0-25 (26 LEDs)
...
```

### Тестовый скетч для инвентаризации

```cpp
// test_led_lines.ino
#include <FastLED.h>

#define NUM_LEDS 300  // Максимально возможное количество
#define LED_PIN 16    // Тестируемый пин

CRGB leds[NUM_LEDS];

void setup() {
  Serial.begin(115200);
  FastLED.addLeds<WS2811, LED_PIN, RGB>(leds, NUM_LEDS);
  FastLED.setBrightness(100);

  // Зажечь все
  fill_solid(leds, NUM_LEDS, CRGB::White);
  FastLED.show();

  Serial.println("Сосчитайте светящиеся пиксели!");
}

void loop() {}
```

## Расширенные возможности

### Создание новой зоны

```cpp
// 1. Определите сегменты
LEDSegment myZone_segments[] = {
  LEDSegment(2, 50, 80),   // Line 2, pixels 50-80
  LEDSegment(3, 10, 40),   // Line 3, pixels 10-40
};

// 2. Добавьте в массив зон
Zone zones[] = {
  // ... существующие зоны
  Zone(15, "My Custom Zone", myZone_segments, 2),
};
```

### Создание эффекта

```cpp
void myCustomEffect() {
  static uint8_t hue = 0;

  for (uint8_t z = 0; z < NUM_ZONES; z++) {
    CRGB color = CHSV(hue + (z * 20), 255, 255);
    ZoneController::setZoneColor(z, color);
  }

  hue++;
  FastLED.show();
}

// В loop():
void loop() {
  myCustomEffect();
  delay(30);
}
```

### Синхронизация зоны и актуатора

```cpp
// При движении актуатора подсветить его зону
void highlightActuatorZone(uint8_t actuatorId) {
  // Найти зону для актуатора
  for (uint8_t i = 0; i < NUM_ACTUATOR_MAPPINGS; i++) {
    if (ACTUATOR_MAPPINGS[i].actuatorId == actuatorId) {
      uint8_t zoneId = ACTUATOR_MAPPINGS[i].zoneId;
      ZoneController::fadeInZone(zoneId, CRGB::Gold, 300);
      break;
    }
  }

  ActuatorController::moveForward(actuatorId, 2000);
}
```

## Оптимизация производительности

### Память

ESP32 имеет ~520KB RAM. Расчет для вашей инсталляции:

```
10 линий × 150 LED (средняя) × 3 байта = ~4.5KB
31 актуатор × структура = ~1KB
Итого: ~6KB для LED + системные структуры
```

**Вывод:** Даже с 1000+ LED у вас будет достаточно памяти.

### Скорость обновления

```cpp
// В loop() используйте таймер для 30-60 FPS
unsigned long lastUpdate = 0;
const uint16_t FRAME_TIME = 33;  // ~30 FPS

void loop() {
  if (millis() - lastUpdate > FRAME_TIME) {
    updateEffects();
    FastLED.show();
    lastUpdate = millis();
  }
}
```

### Параллельные линии FastLED

FastLED автоматически использует параллельный вывод на ESP32 через RMT каналы. Ваши 10 линий будут обновляться **одновременно** - это очень быстро!

## Безопасность

### Защита актуаторов

```cpp
// Автоматический стоп через N секунд
ActuatorController::moveForward(id, 3000);  // Максимум 3 сек

// Принудительная остановка всех при ошибке
if (emergency) {
  ActuatorController::stopAll();
}
```

### Защита от перегрева LED

```cpp
// Ограничение максимального тока (в setup)
FastLED.setMaxPowerInVoltsAndMilliamps(12, 30000);  // 12V, 30A
```

## FAQ

**Q: Сколько памяти займет моя конфигурация?**
A: ~6KB для типичной установки с 1000 LED. ESP32 имеет 520KB RAM.

**Q: Можно ли использовать больше 10 линий?**
A: Да, ESP32 имеет 8 RMT каналов для параллельного вывода, но можно использовать и больше линий с программным битбангингом (медленнее).

**Q: Как синхронизировать свет и движение?**
A: Используйте callback'и или проверяйте состояние актуаторов в loop().

**Q: А если у меня 5V WS2812B вместо 12V WS2811?**
A: Измените в ZONE_CONFIG.h:
```cpp
#define LED_TYPE WS2812B
#define COLOR_ORDER GRB  // Обычно для WS2812B
```

**Q: Нужно ли калибровать актуаторы?**
A: Да, измерьте время полного хода каждого актуатора и обновите `moveDuration` в конфигурации.

## Следующие шаги

1. ✅ Создать базовую архитектуру (ГОТОВО)
2. 🔲 Провести инвентаризацию LED
3. 🔲 Заполнить реальные индексы сегментов
4. 🔲 Протестировать каждую зону отдельно
5. 🔲 Настроить GPIO для всех 31 актуатора
6. 🔲 Создать финальную конфигурацию
7. 🔲 Интегрировать с веб-интерфейсом

## Поддержка

Если у тебя возникнут вопросы по архитектуре или нужна помощь в настройке - пиши. Эта система спроектирована для масштабирования и легко адаптируется под любую геометрию.

**Удачи с инсталляцией!** 🚀
