# 🎬 КАК РАБОТАЮТ МЕДИА-ФАЙЛЫ В RAMS INTERACTIVE HUB

## 📊 ЛОГИКА РАБОТЫ

### 🔍 Где приложение ищет медиа?

**В разработке (на Mac):**
```
public/projects/
└── rams-city/
    ├── video-1.mp4
    └── photo-1.jpg
```

**В production (на Windows):**
```
[Папка с .exe]/media/projects/
└── rams-city/
    ├── video-1.mp4
    └── photo-1.jpg
```

---

## ⚙️ ТЕХНИЧЕСКАЯ ЛОГИКА

### Код в `electron/main.js` (строки 102-143):

```javascript
// Регистрация протокола media://
protocol.registerFileProtocol('media', (request, callback) => {
  const url = request.url.replace('media://', '');
  const isDev = process.env.NODE_ENV === 'development';
  
  let resourcePath;
  if (isDev) {
    // DEV: ищет в public/
    resourcePath = path.join(__dirname, '..', 'public', url);
  } else {
    // PROD: ищет в media/ рядом с .exe
    const appPath = app.getAppPath();
    const mediaPath = path.join(path.dirname(appPath), 'media', url);
    
    // Fallback: относительно exe
    if (!fs.existsSync(path.dirname(mediaPath))) {
      resourcePath = path.join(process.cwd(), 'media', url);
    }
  }
  
  callback({ path: decodedPath });
});

// Функция получения корневой папки медиа
const getMediaRoot = () => {
  if (isDev) {
    return path.join(__dirname, '..', 'public');
  }
  // Production: media/ рядом с .exe
  const exeDir = path.dirname(app.getPath('exe'));
  return path.join(exeDir, 'media');
};
```

---

## 📂 СТРУКТУРА НА WINDOWS

### ✅ ПРАВИЛЬНАЯ СТРУКТУРА:

```
C:\RAMS\
├── RAMS-Interactive-Hub-Portable.exe    ← Запускаемый файл
└── media\
    └── projects\
        ├── rams-city\
        │   ├── video-1.mp4
        │   ├── video-1-poster.jpg
        │   ├── photo-1.jpg
        │   └── logo.png
        ├── rams-beyond\
        │   ├── video-1.mp4
        │   └── ...
        └── ... (все остальные проекты)
```

### ❌ НЕПРАВИЛЬНО:

```
C:\RAMS\
├── RAMS-Interactive-Hub-Portable.exe
└── projects\              ← НЕТ! Нужна папка media/
    └── rams-city\
```

```
C:\RAMS\
├── RAMS-Interactive-Hub-Portable.exe
└── RAMS-Media\            ← НЕТ! Должна называться media/
    └── projects\
```

---

## 🎯 КАК ПРИЛОЖЕНИЕ ЗАГРУЖАЕТ МЕДИА

### 1. Код в компонентах использует протокол `media://`:

```tsx
// Пример в компонентах:
<video src="media://projects/rams-city/video-1.mp4" />
<img src="media://projects/rams-city/photo-1.jpg" />
```

### 2. Electron перехватывает запросы:

```
media://projects/rams-city/video-1.mp4
       ↓
C:\RAMS\media\projects\rams-city\video-1.mp4
```

### 3. Если файл найден → загружается, если нет → ошибка

---

## 📋 ИНСТРУКЦИЯ ПО РАЗВЕРТЫВАНИЮ

### Шаг 1: Скачай .exe с GitHub

✅ Уже сделано! Файлы:
- `RAMS Interactive Hub Setup 0.1.0.exe`
- `RAMS-Interactive-Hub-Portable.exe`

### Шаг 2: Создай структуру папок

**На флешке или на Windows:**

```
C:\RAMS\
└── media\
    └── projects\
```

### Шаг 3: Скопируй медиа-файлы

**С твоего Mac (из проекта):**
```bash
# Исходная папка на Mac:
/Users/nurdauletakhmatov/Desktop/rams-screen/rams-interactive-hub/public/projects/

# Скопируй ВСЮ папку projects на флешку
# Затем на Windows положи в:
C:\RAMS\media\projects\
```

**ИЛИ скачай из облака (если есть):**
- Google Drive
- Dropbox
- Любое облачное хранилище

### Шаг 4: Положи .exe в ту же папку

```
C:\RAMS\
├── RAMS-Interactive-Hub-Portable.exe    ← Сюда!
└── media\
    └── projects\
        └── ... (все проекты)
```

### Шаг 5: Запускай!

Двойной клик на `RAMS-Interactive-Hub-Portable.exe`

---

## 🔄 АЛЬТЕРНАТИВНЫЙ ВАРИАНТ (с внешней базой данных)

Если хочешь управлять проектами динамически:

### Создай файл `projects.json`:

```
C:\RAMS\media\projects.json
```

**Содержание:**
```json
[
  {
    "id": "rams-city",
    "name": "RAMS City Kazakhstan",
    "logo": "media://projects/rams-city/logo.png",
    "scenes": [
      {
        "id": "video-1",
        "type": "video",
        "title": "Обзор проекта",
        "image": "media://projects/rams-city/video-1-poster.jpg",
        "video": "media://projects/rams-city/video-1.mp4"
      }
    ]
  }
]
```

**Приложение автоматически:**
1. Проверит есть ли `C:\RAMS\media\projects.json`
2. Если есть → загрузит проекты оттуда
3. Если нет → использует встроенные данные

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: "Нет картинок / видео"

**Причина:** Медиа не в правильной папке

**Решение:**
1. Проверь путь: `C:\RAMS\media\projects\`
2. Убедись что файлы там есть:
   ```cmd
   dir C:\RAMS\media\projects\rams-city\
   ```
3. Перезапусти приложение (Ctrl+Q → запусти снова)

### Проблема: "Некоторые проекты без медиа"

**Причина:** Медиа для этих проектов не скопированы

**Решение:**
1. Проверь какие проекты есть в `lib/data/projects.ts`
2. Для каждого проекта нужна папка: `media/projects/[project-id]/`
3. Скопируй медиа для недостающих проектов

### Проблема: "Видео не воспроизводится"

**Причина:** Формат не поддерживается или файл повреждён

**Решение:**
1. Убедись что видео в формате MP4 (H.264)
2. Проверь что файл не повреждён (открой в VLC)
3. Переконвертируй если нужно:
   ```bash
   ffmpeg -i input.mov -c:v libx264 -c:a aac output.mp4
   ```

---

## 💾 РАЗМЕРЫ И ОПТИМИЗАЦИЯ

### Размеры файлов:

| Компонент | Размер |
|-----------|--------|
| .exe файл | ~300 MB |
| Медиа (все проекты) | ~5 GB |
| **ИТОГО** | ~5.3 GB |

### Оптимизация:

**Если места мало:**
1. Не копируй все проекты, только нужные
2. Используй видео в меньшем разрешении (1080p → 720p)
3. Сожми изображения (JPEG quality 85%)

**Пример:**
```bash
# Сжать видео до 720p:
ffmpeg -i input.mp4 -vf scale=1280:720 -c:v libx264 -crf 23 output.mp4
```

---

## 📊 ЧЕКЛИСТ РАЗВЕРТЫВАНИЯ

- [ ] Скачал .exe с GitHub Actions
- [ ] Создал папку `C:\RAMS\`
- [ ] Создал папку `C:\RAMS\media\`
- [ ] Создал папку `C:\RAMS\media\projects\`
- [ ] Скопировал все медиа в `C:\RAMS\media\projects\`
- [ ] Положил .exe в `C:\RAMS\`
- [ ] Запустил приложение
- [ ] Проверил что медиа загружаются

---

## ✅ ИТОГО

**Приложение работает по принципу:**

```
.exe файл (легкий, ~300 MB)
    +
media/ папка (тяжелая, ~5 GB)
    =
Полноценное приложение
```

**Плюсы:**
- ✅ Можно обновлять .exe без переноса 5GB медиа
- ✅ Можно добавлять новые проекты без пересборки
- ✅ Быстрое развертывание на нескольких киосках

**Готово!** 🚀
