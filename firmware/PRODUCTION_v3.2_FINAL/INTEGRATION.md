# ESP32 Интеграция с Electron приложением

**Дата:** 2026-02-15
**Версия:** 1.0

---

## 📦 Что добавлено

### 1. ESP32 API Клиент (`lib/esp32-client.ts`)

TypeScript клиент для управления ESP32 контроллером:

- ✅ Управление 15 актуаторными блоками
- ✅ Управление LED (цвет, яркость, скорость, эффекты)
- ✅ Автоматические retry попытки
- ✅ Timeout защита
- ✅ TypeScript типизация

**Использование:**
```typescript
import { createLocalESP32Client } from "@/lib/esp32-client";

const client = createLocalESP32Client(); // 192.168.4.1:80

// Управление актуаторами
await client.blockUp(5, 10000);    // Блок 5 вверх на 10 сек
await client.blockDown(3, 5000);   // Блок 3 вниз на 5 сек
await client.blockStop(7);         // Остановить блок 7
await client.stopAll();            // Остановить все

// Управление LED
await client.setLEDColor(255, 0, 0);      // Красный
await client.setLEDBrightness(200);       // Яркость 200
await client.setLEDSpeed(128);            // Скорость 128
await client.setLEDEffect(2);             // Радуга
```

---

### 2. Панель управления (`components/actuator-control.tsx`)

Полноценный UI для управления системой:

**Вкладка "Актуаторы":**
- 15 блоков с кнопками UP/DOWN/STOP
- Визуальная индикация активных блоков
- Информация о типе блока (OUTER/INNER/FULL)
- Счетчик активных блоков (макс 2)
- STOP ALL кнопка

**Вкладка "LED Эффекты":**
- Color picker (выбор цвета)
- Слайдер яркости (0-255)
- Слайдер скорости анимации (0-255)
- 8 эффектов: Статика, Пульс, Радуга, Бегущий, Искры, Волна, Огонь, Метеор
- Быстрый выбор из 8 preset цветов

**Функции:**
- Автоматическое переподключение к ESP32
- Статус подключения в реальном времени
- Обработка ошибок
- Анимации и transitions

---

### 3. Интеграция в приложение (`app/page.tsx`)

Добавлен новый shortcut: **Ctrl+Shift+C** - открывает панель управления

```typescript
// В любом месте приложения нажмите Ctrl+Shift+C
// Откроется ActuatorControl модалка
```

---

## 🚀 Как использовать

### Шаг 1: Подключение к ESP32

1. Убедитесь что ESP32 включен и прошит firmware v3.2
2. Подключитесь к WiFi сети **"RAMS_Controller"** (пароль: `rams2026`)
3. ESP32 будет доступен по адресу `192.168.4.1`

### Шаг 2: Запуск приложения

```bash
cd rams-interactive-hub
npm install
npm run electron:dev
```

### Шаг 3: Открытие панели управления

- Нажмите **Ctrl+Shift+C** в любом месте приложения
- Или программно:
  ```typescript
  setIsActuatorControlOpen(true);
  ```

### Шаг 4: Управление

**Актуаторы:**
1. Переключитесь на вкладку "Актуаторы"
2. Выберите блок (1-15)
3. Нажмите UP/DOWN для движения или STOP для остановки
4. Используйте STOP ALL для экстренной остановки всех блоков

**LED:**
1. Переключитесь на вкладку "LED Эффекты"
2. Выберите цвет (color picker или preset)
3. Настройте яркость и скорость
4. Выберите эффект

---

## 🎨 LED Эффекты

| ID | Название | Описание |
|----|----------|----------|
| 0  | Статика  | Статичный цвет |
| 1  | Пульс    | Плавное пульсирование |
| 2  | Радуга   | Радужная анимация |
| 3  | Бегущий  | Бегущие огни |
| 4  | Искры    | Случайные вспышки |
| 5  | Волна    | Волновой эффект |
| 6  | Огонь    | Имитация огня |
| 7  | Метеор   | Эффект метеора |

---

## 🔌 API Endpoints (ESP32)

### Актуаторы

```
GET  /api/status                    - Получить статус всех блоков
POST /api/block?num=5&action=UP&duration=10000  - Управление блоком
POST /api/stop                      - Остановить все блоки
```

### LED (только для svetdiod-project)

```
POST /api/color?r=255&g=0&b=0      - Установить цвет
POST /api/bri?v=200                - Установить яркость
POST /api/spd?v=128                - Установить скорость
POST /api/fx?id=2                  - Установить эффект
GET  /api/state                    - Получить текущее состояние LED
```

---

## 🛠️ Разработка

### Добавление нового функционала

**Пример: Добавить preset анимации**

1. Обновите `lib/esp32-client.ts`:
```typescript
async setPreset(presetId: number): Promise<void> {
  await this.setLEDColor(presets[presetId].r, presets[presetId].g, presets[presetId].b);
  await this.setLEDEffect(presets[presetId].effect);
  await this.setLEDSpeed(presets[presetId].speed);
}
```

2. Добавьте UI в `components/actuator-control.tsx`:
```typescript
<button onClick={() => client.setPreset(0)}>
  Preset 1
</button>
```

### Изменение IP адреса ESP32

По умолчанию используется `192.168.4.1` (WiFi AP mode).

Для изменения:
```typescript
import { ESP32Client } from "@/lib/esp32-client";

const client = new ESP32Client({
  host: "192.168.1.100",  // Ваш IP
  port: 80,
  timeout: 5000
});
```

---

## 🐛 Troubleshooting

### ESP32 не подключается

1. Проверьте что вы подключены к WiFi "RAMS_Controller"
2. Проверьте что ESP32 включен (зеленый LED должен мигать)
3. Попробуйте открыть `http://192.168.4.1` в браузере
4. Проверьте Serial Monitor ESP32 на наличие ошибок

### LED не работают

⚠️ **ВАЖНО:** LED управление доступно только если ESP32 прошит firmware из `svetdiod-project/main.cpp`

Если используется `rams_controller_v3.ino` - LED управляются только через actuat блоки (UP/DOWN команды автоматически включают LED).

Для полного LED контроля нужно прошить `svetdiod-project`:

```bash
cd /Users/nurdauletakhmatov/Desktop/rams-screen/svetdiod-project
pio run --target upload
```

### Блоки не двигаются

1. Проверьте подключение Serial кабелей (ESP32 ↔ Mega)
2. Проверьте Serial Monitor Mega #1 и Mega #2
3. Убедитесь что Mega платы получают команды (должны выводить "[RX] BLOCK:...")
4. Проверьте питание актуаторов

---

## 📝 TODO

- [ ] Добавить preset'ы для LED (готовые сцены)
- [ ] Сохранение настроек LED в localStorage
- [ ] Синхронизация LED с музыкой
- [ ] Группы блоков (включить несколько блоков одной кнопкой)
- [ ] Таймлайн анимаций (последовательность действий)
- [ ] Экспорт/импорт конфигураций
- [ ] Remote control через WebSocket

---

## 🎉 Готово!

Теперь у тебя есть полноценная панель управления актуаторами и LED эффектами прямо в Electron приложении!

**Горячие клавиши:**
- `Ctrl+Shift+A` - Admin panel
- `Ctrl+Shift+C` - Actuator Control panel
